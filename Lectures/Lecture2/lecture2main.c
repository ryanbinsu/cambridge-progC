#include <stdio.h>
#include <string.h>
#include "lecture2.h"

// Question 3
// #define SWAP(t, x, y) \
// do {                  \
//     t tmp;            \
//     tmp = x;          \
//     x = y;            \
//     y = tmp;          \
// } while(0)

// Question 5
// #define SWAP(x, y)      \
// do {                    \
//     (x=x+y,y=x-y,x=x-y;)\
// } while(0)

int main(void) {
    // Question 1:
    // char s[] = "Count me";
    // printf("%d\n", cntlower(s));
    // return 0;

    // Question 2:
    // int arr[] = { 12, 11, 13, 5, 6, 7 };
    // int arr_size = sizeof(arr) / sizeof(arr[0]);

    // mergeSort(arr, 0, arr_size - 1);

    // printf("%d\n", arr[1]);
    // return 0;
}